<div class="filter" id="filter-{{filter}}">
    <div class="filterName">
        <p>{{filter}}</p>
    </div>
    <div class="filterExit" id="parent-{{filter}}">
        <i class="fas fa-times" id="child-{{filter}}"></i>
    </div>
</div>